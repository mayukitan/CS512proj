clc;
clear;
%%
train = tdfread('training_all.txt', '\t');
truth = tdfread('ground_truth_all.txt', '\t');
%%
train_cell = cell(length(train.actor),4);
train_cell(:,1) = cellstr(train.actor);
train_cell(:,2) = cellstr(train.repo_name);
train_cell(:,3) = cellstr(train.repo_owner);
train_cell(:,4) = cellstr(train.language);

%%
truth_cell = cell(length(train.actor),4);
train_cell(:,1) = cellstr(train.actor);
train_cell(:,2) = cellstr(train.repo_name);
train_cell(:,3) = cellstr(train.repo_owner);
train_cell(:,4) = cellstr(train.language);
